# Main and social menus are defined per-language in config/_default/languages.toml.
# Defining menus under [languages.<code>.menus] overrides site-level menus, so
# entries here would be ignored. Icons are from https://tabler.io/icons.
